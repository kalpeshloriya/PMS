
services:
  - type: web
    name: pms-app-fast
    env: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements-render.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
